import { NextRequest, NextResponse } from "next/server";
import { requireAdmin } from "@/lib/admin/auth";
import { createServiceRoleClient } from "@/lib/supabase/server";
import { loadRevenueAnalytics, loadWebsiteAnalytics } from "@/lib/revenue-os/analytics";
import { isMissingRevenueSchema } from "@/lib/revenue-os/db";
export async function GET(request: NextRequest) { const auth = await requireAdmin(); if (auth instanceof NextResponse) return auth; const days = Math.min(365, Math.max(7, Number(new URL(request.url).searchParams.get("days")) || 30)); try { const supabase = createServiceRoleClient(); const revenue = await loadRevenueAnalytics(supabase, days); try { return NextResponse.json({ schemaReady: true, ...revenue, web: await loadWebsiteAnalytics(supabase, days) }); } catch { return NextResponse.json({ schemaReady: true, ...revenue, web: { status: "degraded", reason: "First-party event storage is not ready. Apply the analytics migration; no vendor key is required.", pageViews: null, visitors: null, conversions: null, conversionRate: null, topPages: [], sources: [], conversionEvents: [], eventCount: null, lastCapturedAt: null } }); } } catch (error) { if (isMissingRevenueSchema(error)) return NextResponse.json({ schemaReady: false, funnel: null, sources: [], web: null }); return NextResponse.json({ error: "Could not load canonical analytics" }, { status: 500 }); } }
