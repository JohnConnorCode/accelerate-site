import { NotFoundBody } from "@/components/layout/NotFoundBody";

/** Marketing layout already provides chrome. Do not wrap this again. */
export default function MarketingNotFound() {
  return <NotFoundBody />;
}
