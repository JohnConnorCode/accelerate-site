# Revenue OS module map

The full architecture and invariants live in
`docs/contracts/REVENUE-OS-ENGINEERING-CONTRACT.md`. The platform vision and
product layers live in `docs/NORTHSTAR.md`. This directory is the authoritative
domain layer. API routes, pages, cron handlers, webhooks, and AI tools should
call these modules instead of recreating their rules.

The northstar defines six runtime primitives that will extend this module map as
Phase B and C land (see `docs/NORTHSTAR.md` §6–§20 and the Feature Board cards
`durable-work-engine`, `capability-graph-canonical`, `evidence-claim-ledger`,
`autonomy-policy-engine`, `coworker-model`, `agent-activity-surface`):
- **WorkItems** — durable, lease-based, retryable, explainable work (generalises `tasks.ts` + `scheduler.ts`)
- **Capability Graph** — canonical workspace capability resolution (extends `integration-registry.ts`)
- **Evidence & Claim Ledger** — evidence-backed facts with human truth hierarchy
- **Autonomy Policy Engine** — five-level autonomy ladder with hard safety floors (generalises `actions.ts` + `ai-tools.ts` impact tiers)
- **Coworkers** — first-class identities with manifests over the shared runtime
- **Agent Activity** — readable agent timelines on every record and in the Command Center

| Module                                        | Authoritative responsibility                                                                                                 | Typical callers                                                                       | Invariants / verification                                                                                                                                     |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `identity.ts`                                 | Contact/company resolution and ambiguity refusal                                                                             | Inbound, Google sync, Pipeline create                                                 | Canonical-ID precedence; exact primary/alternate email helper; never name-only merge; identity backlog/tests                                                  |
| `inbound.ts`                                  | Canonical capture, attribution, activity, immediate task                                                                     | Forms, chat, qualifier, manual Lead adapter                                           | Idempotent source ID; confirmation failure cannot discard inquiry                                                                                             |
| `auto-responder.ts`                           | Approved-policy-only automated first-touch reply to inbound inquiries                                                        | `inbound.ts`                                                                          | Version-pinned founder approval; fixed context envelope/allowlist; grounding checks block pricing/promises/dates before send; every decline audited           |
| `pipeline.ts`                                 | Controlled stage transition                                                                                                  | Pipeline API, AI executor, inbound, future calendar/proposals                         | Valid transition, loss reason, stage event, audit, optimistic concurrency                                                                                     |
| `tasks.ts`                                    | Deduplicated operator commitments                                                                                            | Today APIs, inbound, AI executor, meeting/campaign workflows                          | Stable dedupe key; audit/activity; no duplicate open task                                                                                                     |
| `activities.ts`                               | Normalized cross-channel activity receipts and bounded record timelines                                                      | Inbound, pipeline, tasks, communications, provider adapters, record details, AI tools | Stable `(source, external_id)` replay boundary; canonical links; deterministic occurred-at ordering; provider facts remain immutable                          |
| `conversations.ts`                            | Omnichannel conversation listing, filtering, triage, status updates, and record linking                                      | Admin conversations page, conversations API                                           | Valid statuses, unread count reset on resolve/archive, deduplicated task creation, opportunity linking with audit receipts                                    |
| `records.ts`                                  | Bounded opportunity cockpit read model                                                                                       | Opportunity record API/admin, future contextual AI reads                              | Curated fields only; canonical activity reader; linked work is bounded; no provider payloads, proposal bodies, attendee payloads, or secrets                  |
| `communications.ts`                           | Auditable Resend delivery                                                                                                    | Campaigns, confirmed email actions, transactional adapters                            | Claim before send; provider ID/status; uncertain outcome is not blind retry                                                                                   |
| `recovery.ts`                                 | Recovery playbook validation, eligibility classification, and read-only audience preview                                     | Admin recovery page, recovery API, revenue-campaigns cron                             | Only supported template variables; excludes suppressed/active-opportunity/already-recovering contacts; preview always precedes staging                        |
| `recovery-copy.ts`                            | Founder-editable starting templates for the five bounded recovery motions                                                    | `recovery.ts`, admin recovery page                                                    | Templates use only campaign-executor-resolved variables                                                                                                       |
| `google.ts`                                   | OAuth, Gmail/Calendar/Drive sync, threaded Gmail send                                                                        | Google routes, cron, confirmed actions                                                | Encrypted tokens, exact attendee/contact association, cursor/provider IDs, folder bounds                                                                      |
| `google-oauth.ts`                             | Signed/expiring OAuth state binding and operator-facing Google error classification                                          | Google authorize/callback/status/sync routes                                          | HMAC-signed state with timing-safe comparison; expired/mismatched state rejected; errors mapped to fixed operator codes, no leaking raw provider errors       |
| `gmail-reply-mime.ts`                         | RFC 5322 reply headers and raw MIME body for threaded Gmail sends                                                            | `google.ts`                                                                           | Message-ID/References threading preserved; throws rather than sending a reply with no thread anchor                                                           |
| `gmail-sync-plan.ts`                          | Pure planning for bounded incremental/initial/recovery Gmail thread sync                                                     | `google.ts`                                                                           | Deterministic thread ordering; cursor only advances when the bounded page has no remainder                                                                    |
| `encryption.ts`                               | AES-256-GCM envelope encryption for stored provider secrets and tenant-scoped credentials                                    | `google.ts`, tenancy providers route, `openrouter-credentials.ts`, `resend.ts`        | Versioned envelope, unknown versions fail closed; tenant-scoped secrets bind AAD to tenant/provider/field so a copied value can't decrypt elsewhere           |
| `integration-registry.ts` / `integrations.ts` | Versioned provider capabilities and the evidence-backed integration catalog                                                  | Integrations API/admin, Setup, future AI availability gates and adapters              | Configuration is never health; planned providers stay unavailable; scopes, freshness, failures, cost posture, and provider boundaries remain explicit         |
| `setup-status.ts`                             | Pure readiness classification for Resend, Calendly attribution, and campaign engine                                          | Admin setup route                                                                     | Configuration is never conflated with delivery/attribution health; explicit ready/action/degraded/optional/disabled states                                    |
| `health.ts`                                   | Single operational health computation across integrations, source runs, job runs, webhook failures                           | Admin overview, Setup Center, cron health snapshot                                    | Stalled `running` jobs surfaced explicitly; unread webhook failures surfaced; health is never "ready" solely because nothing else reported                    |
| `alerts.ts`                                   | Deduplicated operational alerting to `admin_notifications`, emailed only for costly kinds                                    | `runs.ts`, `integration-registry.ts`, cron/webhook failure paths                      | One unread notification per condition (23505 dedupe); email fires once until acknowledged; alerting failures never fail the caller                            |
| `scheduler.ts`                                | Deterministic cadence keys and redacted scheduled-job summaries                                                              | Supabase Cron wake-up adapter and future proactive jobs                               | Postgres owns cadence only; authenticated routes call services; `withJobRun` owns claims and receipts                                                         |
| `work-items.ts`                              | Durable, lease-based, retryable, explainable work items (northstar Phase B1)                                                 | Coworkers (Phase C), AI tools, future autonomous workers                              | `claim_work_item` RPC + advisory lock; dedupe-on-write; stale claim recovery; mandatory reason on every item and scheduled check; activity + audit receipts   |
| `capabilities.ts`                            | Canonical workspace capability resolution (northstar Phase B2)                                                                | AI tools, coworkers, capability pre-checks, integration-registry sync                 | One capability key per tenant; `resolve_workspace_capability` RPC for atomic reads; `checkCapabilitiesBeforeWork` for pre-flight validation; upsert for sync |
| `claims.ts`                                  | Evidence-backed fact system with human truth hierarchy (northstar Phase B3)                                                   | AI tools, entity record context, identity enrichment, conflict resolution            | `record_evidence` RPC for atomic evidence+claim writes; human truth hierarchy (human_confirmed > human_entered > verified_external > probable_external > model_inference); conflict detection on value divergence; retract/supersede/human-confirm operations |
| `autonomy-policy.ts`                         | Five-level autonomy ladder with hard safety floors (northstar Phase B4)                                                       | AI tools, action executor, coworker pre-checks, standing approval grants             | `check_autonomy` RPC with hard-floor-first evaluation; coworker-specific policies; standing permission upgrades with approval provenance; fail-closed default (always_ask) |
| `coworkers.ts`                               | Explicit worker identities with manifests over the shared runtime (northstar Phase B5)                                        | Work engine, claims, AI tools, agent context                                         | First-class configuration objects (not separate LLMs); required capabilities checked via capability graph; work_kinds routing; manifest includes readiness gaps |
| `agent-activity.ts`                          | Readable agent activity surface on every record (northstar Phase B6)                                                          | AI tools, record detail views, Command Center                                        | Composes work items + actions + claims + audit into a human-readable timeline; not a raw audit dump — makes autonomous work understandable to a normal operator |
| `sales-coworker.ts`                          | Reference coworker: qualifies leads, drafts follow-ups, monitors stale proposals (northstar Phase C)                          | Inbound capture, AI tools, cron stale-proposal scanner                               | 5 work kinds (qualify_lead, draft_followup, review_stale_proposal, gather_lead_context, schedule_followup_check); autoplug into inbound flow; bootstrap registers capabilities + autonomy policies |
| `work-executor.ts`                           | Claims and executes claimable work items on the cron schedule                                                                 | Work engine cron route (`/api/cron/work-engine`)                                     | Handler registry pattern; stale claim recovery; coworker readiness + autonomy checks before execution; learned policy check (blocks on prohibitive rules); stores agent memory after each successful execution; audit receipts per cycle |
| `work-scheduler.ts`                         | Auto-creates recurring daily/weekly work items on each cron cycle; scans upcoming meetings for pre-call briefs (northstar Phase E)                                        | Work engine cron route, before `executeClaimableWork`                                | Date-based dedupe keys prevent duplicates; daily items (digest, stale deals, bottleneck, velocity change, health check, integration audit, data quality, overdue scan, revenue stage audit); weekly items (reconciliation on Mondays); scans calendar_events 48h ahead for meeting briefs; surfaces daily operator check-in task |
| `plugins.ts`                                 | Plugin SDK: manifest registration, tool/trigger registry, governance integration (northstar Phase D)                         | AI tools, MCP server, admin plugin management                                        | Plugin manifest with capability requirements, tool declarations, trigger registrations, and permission declarations; all plugin tools flow through autonomy policy engine; registerPlugin auto-registers capabilities + autonomy policies |
| `mcp-client.ts`                              | MCP client connector for consuming external MCP servers (Jira, Linear, Slack, etc.)                                          | Plugin registration, work kind handlers                                              | Discovery (initialize + tools/list) and execution (tools/call); discovered tools must be registered as plugin_tools before use; MCP tools never bypass Accelerate governance |
| `business-pulse-coworker.ts`                 | Business Pulse Coworker: pipeline health monitoring, anomaly detection, daily digests (northstar Phase E, priority 1)        | Work engine cron, AI tools                                                           | 4 work kinds (daily_digest, detect_stale_deals, detect_stage_bottleneck, detect_velocity_change); stale = 7+ days; bottleneck = >40% in one stage; velocity alert = >50% WoW change |
| `meeting-intel-coworker.ts`                  | Meeting Intelligence Coworker: pre-call briefs, post-meeting processing, CRM updates (northstar Phase E, priority 2)         | Work engine cron, AI tools, calendar sync                                            | 3 work kinds (pre_call_brief, post_meeting_process, update_crm_from_meeting); briefs gather contact+opportunity+activity context; post-meeting queues CRM update |
| `finance-coworker.ts`                        | Finance Coworker: revenue tracking, overdue payment detection, financial reconciliation (northstar Phase E, priority 3)     | Work engine cron, AI tools                                                           | 3 work kinds (weekly_revenue_reconciliation, detect_overdue_payments, revenue_stage_audit); reconciles won deals vs prior week; flags stale won deals as overdue; identifies high-stage deals without recent activity |
| `operations-coworker.ts`                     | Operations Coworker: system health monitoring, integration auditing, data quality scanning (northstar Phase E, priority 4)  | Work engine cron, AI tools                                                           | 3 work kinds (daily_health_check, integration_status_audit, data_quality_scan); checks failed jobs + stale claims + expired actions; audits source_runs for failures; scans contacts/opportunities for missing critical fields |
| `memory.ts`                                  | Unified memory architecture across five categories (northstar §23)                                                          | AI tools, coworkers, agent context                                                   | 5 categories (canonical, activity, knowledge, agent, learned_policy) kept distinct; agent_memory table for prior work/research/questions with time-decay horizons; learned_policies table for explicit rules from human decisions with supersession; unified queryMemory routes across all categories without collapsing them |
| `budgets.ts`                                | Resource constraints for autonomous workers (northstar §24)                                                                 | Work executor, AI tools, admin budget management                                    | Per-coworker or global limits (model_spend, vendor_api_calls, emails_sent, research_depth, retry_count, runtime_seconds); daily/weekly/monthly periods; checkBudgets gates work execution; 90%+ usage triggers audit alert; running out of budget is a normal state |
| `notes.ts`                                    | Idempotent founder knowledge capture, bounded retrieval, and privacy-safe adoption evidence on the canonical activity ledger | Global admin note composer, knowledge readers, burn-in report                         | No parallel note store; exact canonical attachment only; content stays out of audit and adoption payloads                                                     |
| `knowledge.ts`                                | Grounded knowledge retrieval with provenance across canonical records, founder notes, and activities                         | AI tools (`search_knowledge_base`), Second Brain substrate                            | Contract `revenue-os-knowledge.v1`; refuses cleanly on no match; tags recency/confidence; surfaces discrepancies where prose conflicts with canonical records |
| `campaigns.ts`                                | Policy/version activation, pause, due execution                                                                              | Campaign APIs, cron, action executor                                                  | Approved version only; JIT send; stop rules re-read before send                                                                                               |
| `campaign-stops.ts`                           | Canonical campaign eligibility stops and pre-send recheck                                                                    | Unsubscribe, Resend webhooks, campaign executor                                       | One reason/state mapping; pending memberships stopped before future sends; no route-local suppression writes                                                  |
| `actions.ts`                                  | Proposed action lifecycle, atomic claims, state invalidation, and expiry sweep                                               | AI tools, action APIs, Today approvals                                                | Expiry/dedupe, explicit founder confirmation, one claimant, immutable decision history, automatic expiry sweep                                                |
| `action-executor.ts`                          | Dispatch approved action with state freshness and safety pre-checks to normal services                                       | Action API/worker, Today approvals                                                    | Registered action only; underlying record state/version freshness verified before mutation; pre-send suppression checks; terminal executed/failed result      |
| `queue.ts`                                    | Explainable operator priority                                                                                                | Today, overview, AI snapshot                                                          | Deterministic reasons/tie-break; no opaque AI deadline ranking                                                                                                |
| `analytics.ts`                                | Canonical funnel/revenue plus first-party context                                                                            | Analytics APIs/pages, AI reads                                                        | Explicit window/cohort; unknown attribution visible; shared formulas                                                                                          |
| `runs.ts`                                     | Atomic job claims and job/source terminal receipts                                                                           | Cron, sync, health                                                                    | One active logical job; idempotent replay returns its receipt; truthful success/partial/skipped/failed summary/error/cursor retained                          |
| `audit.ts`                                    | Material actor/origin/before-after ledger                                                                                    | Every mutation service                                                                | No secrets/raw tokens; observable audit failure policy                                                                                                        |
| `contact-imports.ts`                          | Ad hoc source analysis, review digests, approval, claims, and row receipts                                                   | Contact Import API/UI                                                                 | OpenRouter proposes only; exact reviewed snapshot; deterministic identity; no opportunity/send side effects                                                   |
| `ai-tools.ts`                                 | Versioned tool registry, schemas, impact tiers                                                                               | `ai-agent.ts`                                                                         | Unknown tools fail closed; writes/actions require policy confirmation                                                                                         |
| `ai-agent.ts`                                 | Bounded model loop, deterministic tool-pack selection, streaming callbacks, and trace                                        | Shared admin AI stream API                                                            | Registered tools only; bounded turns/context; run/event receipts; page context is server-curated                                                              |
| `ai-context.ts`                               | Model context contract: source allowlists, conversation/tool-result bounding, and grounded-answer validation                 | `ai-agent.ts`, public chat route                                                      | Bounded conversation/tool-result chars; every factual claim must cite an executed tool receipt; ungrounded answers rejected                                   |
| `ai-conversations.ts`                         | Founder-owned persistent AI threads and replay-safe messages                                                                 | AI conversation and stream APIs                                                       | Actor isolation; bounded history; `(conversation_id, client_message_id)` replay boundary; no provider payload storage                                         |
| `ai-stream-contract.ts`                       | Versioned browser-safe command event protocol                                                                                | AI stream API, global panel, Today, AI workspace                                      | Sanitized deltas/tool evidence/proposal notices only; no raw tool output or secrets                                                                           |
| `ai-operations.ts`                            | Filtered/paginated AI run history and single-run detail reads, with redaction                                                | Admin AI runs API routes                                                              | Cursor cannot skip/duplicate rows; every preview/error field passes through secret redaction; missing schema degrades explicitly rather than fails            |
| `ai-operations-contract.ts`                   | Shared types/status enums for AI run history, detail, and capability payloads                                                | `ai-operations.ts`, AIRunHistory/AICapabilities admin UI                              | Single source of run/capability shape; no duplicate type definitions across API and UI                                                                        |
| `agent-trace.ts`                              | Single writer for the `agent_runs` job ledger and event trace, including streamed-response teeing                            | Admin AI stream API, public chat route                                                | One insert/update path per run; failed trace writes are logged and swallowed, never break the caller; stream cancel/disconnect closes the run                 |
| `agent-learning.ts`                           | Immutable rating and safe aggregate signals                                                                                  | AI feedback API/agent context                                                         | No raw feedback promotion; 90-day aggregate only                                                                                                              |
| `legacy-adapter.ts`                           | Temporary source-to-canonical linkage                                                                                        | Specialized admin APIs                                                                | Preserve source IDs; safe degradation; retire only after reconciliation                                                                                       |
| `legacy-reconciliation.ts`                    | Read-only canonical-identity reconciliation report for legacy capture sources                                                | `scripts/legacy-reconciliation-report.ts`                                             | Matches only through existing canonical identity rules; report-only, no writes                                                                                |
| `modules.ts`                                  | Authoritative pluggable module / plugin registry contract separating core from optional business capabilities                | Admin navigation, AI tools, tenant config, setup                                      | Core modules immutable; disabling removes nav/tools/routes cleanly without dangling references; default enabled preserves 100% backward compatibility         |
| `mcp-server.ts`                               | Model Context Protocol (MCP) server exposing tools, resources, and prompt templates to external assistants                   | Claude Desktop, Claude CLI, Cursor, ChatGPT Actions, `/api/mcp` route                 | Same safety/impact-tier gates as internal AI tools; mutations enter `action_queue` for founder confirmation; bounded resources                                |
| `integration-adapters.ts`                     | Standardized adapter SDK for third-party channels (HubSpot CRM batch import, WhatsApp messaging ingress)                     | Inbound webhooks, CRM sync jobs, manual imports                                       | Deterministic identity resolution; immutable activity receipts; replay-safe deduplication                                                                     |
| `schema-contract.ts`                          | Declarative minimum deployed database shape every service may depend on                                                      | `supabase/server.ts`, admin setup route, schema verification CLI                      | Versioned contract; tenant-scoped table allowlist; CLI validates metadata, app validates runtime usability                                                    |
| `db.ts` / `types.ts`                          | Shared normalization, schema errors, contracts                                                                               | All domain modules                                                                    | No route-local stage or normalization variants                                                                                                                |

## Choosing a module

- New person/business matching extends `identity.ts`.
- New import format or contact cleanup policy extends `contact-imports.ts`; approved identity writes still extend `identity.ts`.
- New pipeline movement extends `pipeline.ts`.
- New task source extends `tasks.ts`.
- New message channel extends the communication contract; do not send directly.
- New scheduled/integration work wraps execution with `runs.ts`; use one stable
  job key and a deterministic claim/replay key whenever the invocation itself
  may repeat. Do not hand-roll cron locks in a route.
- New AI capability first needs a normal domain service, then a registered tool.
- Every admin AI surface uses the shared stream API and conversation service; do
  not create route-local chats, model loops, or transcript stores.
- All model traffic uses `src/lib/ai/openrouter.ts` with an explicit tenant-bound database so `openrouter-credentials.ts` can resolve that workspace's encrypted key; do not add a route-local provider SDK or unscoped production call.
- New metric extends `analytics.ts`; screens do not calculate competing funnels.
- New compatibility read extends `legacy-adapter.ts` and names its retirement
  reconciliation card.

Tests do not yet cover every invariant above. The Feature Board cards
`revenue-os-tests`, `api-contract-tests`, and the scoped Playwright cards are the
source of truth for those gaps. Do not describe planned coverage as passing.
