---
title: "Change workspace settings"
description: "Change a notification or your workspace's look, then confirm the change actually saved."
updated: "2026-09-12"
---

Open `/admin/settings` to manage notification preferences and the configuration exposed to your account, from what alerts you get to what your whole workspace looks like.

<DocsFigure
  src="/images/docs/workspace/settings.png"
  alt="The Settings screen in the fictional Northline Roofing workspace."
  width={1440}
  height={1000}
  caption="Settings in the fictional Northline Roofing workspace. This is the real interface with fictional demo data."
/>

## Change one setting

1. Find the setting and read its description. Confirm the current workspace before editing.
2. Change the value or notification preference using its control.
3. Save if the control provides a save action. Wait for the request to finish before making another change.
4. Reload the page and confirm the saved value. For a notification preference, check the next relevant event before assuming delivery works.

Notification categories include new inquiries, hot leads, proposal views, overdue tasks and expiring contracts. A preference controls which notifications are requested; delivery also depends on the relevant connection and service.

## Choose or create an appearance

Open the appearance control in the workspace navigation to choose Paper, Night,
Signal, Studio, Frost, Material 2026 or macOS. The preference stays on this device. Colors, corners,
controls and dialogs follow the selected theme together. Each demo business keeps a separate preference, so trying Studio in Northline Roofing will not change the appearance of your live workspace or the demo launcher. Your choice survives navigation and refresh within that demo session.

Each appearance changes the character of the same workspace:

| Appearance    | What changes                                         |
| ------------- | ---------------------------------------------------- |
| Paper         | Warm paper, white surfaces and crisp cobalt actions  |
| Night         | Graphite surfaces with warm brass accents            |
| Signal        | Midnight teal, precise corners and monospaced labels |
| Studio        | Editorial headings, warm canvas and clay accents     |
| Frost         | Icy translucent surfaces and cool light              |
| Material 2026 | Matte tonal surfaces and soft, rounded controls      |
| macOS         | Silver navigation, system type and tighter controls  |

The picker previews each palette and shape. With a theme focused, use arrow keys
to try adjacent choices, Home or End to reach the first or last choice, and Escape
to close the picker. Your choice applies immediately.

Choose **Comfortable** or **Compact** under **Density** in the same appearance
control. Comfortable gives rows and panels more breathing room; Compact fits more
work into the available space. The choice applies immediately and stays in this
browser when you reload or switch themes. Touch controls keep their usable target
size in either setting. If browser storage is unavailable, the choice works for
the current page; choose it again after reopening the workspace.

Choose **Create a theme** to open **Branding**. Start from an existing appearance,
then adjust its palette, typography, corners and depth. The preview shows your
edits before they affect the workspace. Choose **Save and use theme** to save the
workspace definition and select it on this device. Other members can select the
saved theme from their appearance control.

You can also describe a style under **Create with AI**, such as “warm ivory with
forest green accents.” With the workspace AI connection configured, the assistant
prepares a branding proposal for review. Check the exact proposed change and
approve it before it saves; normal model usage costs apply. Select the saved
workspace theme in Appearance to use it. The fictional demo does not call a paid
model. Manual editing and importing work without an AI connection.

**Import or export a theme** lets you reuse a JSON definition between workspaces.
Import previews the definition; it does not save it. Invalid fields and text
contrast below 4.5:1 are rejected. Adjust the named colors and try again. One
custom workspace theme is stored at a time; export a design before replacing it
if you want to keep a copy. **Reset edits** restores the saved branding. If another
person saves first, reload and review their version before applying your edits.

## Connect a provider

Use [Integrations](/docs/workspace/integrations) for provider credentials and connection setup. General settings reject values that resemble secrets. If a credential field is missing, follow the connection guide rather than placing the key in a text setting.

## Recovering from a failed save

Reload to check the actual stored value. If it stayed unchanged, record the setting name and the error for your technical owner, leaving out the value itself whenever it contains private business information.

Use [Setup Center](/docs/workspace/setup) to inspect a connection problem and [Activity](/docs/command-center/activity) to investigate who changed configuration.

## Inspect layout settings

The **Layout** section shows the current sidebar and Today page order. “Using the default order. No override applied” means there is no saved customization. **Revert to previous** is available only when a previous change exists.

For example, open **Settings** in a fictional demo workspace and inspect both layout entries. Both use the default order and have no previous change to revert. This inspection does not create a saved layout or change a real workspace.
