---
title: "AI tool reference"
description: "Every registered tool, generated straight from the registry: what it needs and what it does."
updated: "2026-09-19"
---

When an assistant needs to answer a question or prepare a change, its registered tools define the operations it can request. This catalog is generated from the current registry and includes each tool’s description and input schema, so a builder can inspect the contract behind a request.

Read tools retrieve available context. Proposal tools prepare changes for the workspace’s review process; some approved operations can contact external providers or recipients. Inspect the specific tool and its requirements rather than assuming that every proposal has the same effect.

Availability depends on workspace access, enabled modules, connected providers and action policy. Use [Setup Center](/docs/workspace/setup) to investigate a missing connection, [Approvals](/docs/command-center/approvals) to understand a proposed change, and [MCP clients](/docs/extend/mcp-clients) to connect an external assistant.

<Callout type="info" title="Find an exact operation">
  Use documentation search for a tool name, then expand its input schema below. An entry describes a
  supported operation; its action result records what happened in a particular run.
</Callout>

<DocsAiToolCatalog />
