---
title: "Import contacts"
description: "Bring in a CSV or pasted list through a review batch that writes nothing until you confirm it."
updated: "2026-09-12"
---

A list import is how a trade-show sign-up sheet or a spreadsheet somebody has been maintaining for years actually becomes usable customer data instead of a file nobody opens again. Every import lands in a review batch first, never writing a contact directly, so start with a small sample list rather than your full file the first time, purely so you can see how the system interpreted your columns before you trust it with everything.

**Before you start:** use a workspace you are authorized to update. AI-assisted analysis needs the configured model path to be available. Check [Setup Center](/docs/workspace/setup) if analysis is unavailable.

<DocsFigure
  src="/images/docs/contacts/import.png"
  alt="The contact import screen in the fictional Northline Roofing workspace."
  width={1440}
  height={1000}
  caption="Contact intake in the fictional Northline Roofing workspace, where an imported list becomes a review batch. Fictional demo data."
/>

## 1. Prepare your list

Use clear column names. This fictional CSV is a starting example:

```csv
fullName,email,phone,companyName,website
Alex Morgan,alex@example.test,,Example Company,https://example.test
Jamie Chen,jamie@example.test,,Example Company,https://example.test
```

A reviewed contact needs a name and at least an email address or phone number. An email must be valid. A supplied website must be a valid domain or HTTP(S) address. Include a business website when you want to identify a company reliably.

## 2. Analyze the source

Open **Contacts** and start an import, or open `/admin/contact-imports`. Upload the file or paste the list, then choose **Analyze and build review**.

The proposed rows are a review snapshot. Analysis has not imported them yet.

## 3. Review the rows

Check names, email addresses, phone numbers, company information, and each row's import decision. Correct errors and exclude rows you do not want. Inspect possible matches rather than assuming similar names identify the same person.

Company information can remain unlinked when there is not enough evidence to identify the business. Resolve warnings deliberately; do not invent missing fields.

## 4. Confirm the import

Save your review changes, then choose **Review approval**. Inspect the exact snapshot and choose **Approve and import** only when it is correct.

## 5. Verify the result

Check the batch result and open a sample of the accepted contacts. Failed rows remain available with a reason. Inspect the existing batch before resubmitting the same file.

Each approved row saves its contact, company changes and receipt together. An interrupted batch can continue from its saved rows; repeating an imported row returns its original receipt. Existing human-maintained contact fields remain intact, and saving a revised partial batch preserves its imported rows.

Older approvals may ask you to save and approve the review again so company matching is tied to the reviewed website or email. Follow that specific message before retrying. If an older batch is still marked executing and cannot be edited, retain it for reconciliation; do not submit a duplicate file to work around the state.

Importing contacts does not send email, subscribe them to a campaign, or create an opportunity. Those are separate actions.
