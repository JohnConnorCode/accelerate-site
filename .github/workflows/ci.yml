name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  # Merge refs are reused across PR runs. Include the SHA so a newer merge
  # request cannot cancel the build that is providing the required check.
  group: ci-${{ github.workflow }}-${{ github.ref }}-${{ github.sha }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify repository hygiene
        run: npm run verify:oss

      - name: Verify architecture contract
        run: npm run verify:agent-contract

      - name: Verify copy guardrails
        run: npm run verify:guardrails

      - name: Verify no fabricated claims
        run: npm run test:no-fabricated-claims

      - name: Verify house style (no em dashes)
        run: npm run test:house-style-copy

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint -- --max-warnings=0

      - name: Check formatting
        run: npm run format:check

      - name: Run core contract suite
        run: npm run test:core

      - name: Build
        run: npm run build
