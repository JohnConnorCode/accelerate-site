# Admin navigation recovery, 12 September 2026

The existing link and shell URL adapters now share one pure module so tests call
the exact functions used by the mounted application. Their behavior is unchanged;
Next.js continues to own routing. AdminLink retains its existing exported helper.

The retained language repair supplies canonical names, grouping, Help and query-aware
Architect identity. This continuation closes the missing scenario/configuration proof.

## Checked locally

The existing semantic language test passes 328 route cases across the live admin,
a tenant URL and six actual scenario packs. It checks canonical identities, root
breadcrumbs, search visibility, module ownership, detail URLs and default Today.
Disabling campaigns hides Email Sequences and restores its stable name when enabled;
core commands remain enabled. All comparisons use the same module and role inputs,
preserving stricter live platform authorization.

Scoped lint, syntax, whitespace and agent-contract checks pass. Independent source
review confirmed the extracted adapters preserve behavior and the new browser
selectors match the actual shell and fictional module endpoint.

## Browser verification

The existing language QA discovers all six businesses from the actual launcher.
At 1440px and 390px, it checks direct Email Sequences, keyboard search to Architect,
visible current navigation, Back, fictional module disable/re-enable, reload,
overflow and console/protected-request isolation. It retains screenshots alongside
the existing theme/Help matrix. Exact remote CI and opened screenshots are required
before final acceptance; no live database or production browser proof is claimed.

CI run 34713079112 passed all 126 language and scenario cases on source
69590f669f8baed4d9777a94ebb2f2d3017f52b9. Opening its retained screenshots
revealed that mobile drawer and command-palette exit animations were still running
at capture. The fixture now waits for those elements to detach. This changes only
verification timing; refreshed screenshots must be inspected before acceptance.
The canonical card evidence and PR record the final run and visual review.

## Documentation review

The communication standard now requires this scenario/configuration matrix. Existing
public guide, changelog and Command Center/FAQ descriptions remain accurate: this
change consolidates unchanged URL adapters and proves the already-described shared
navigation. It adds no screen, configuration option, permission or provider behavior.
