# Services recovery, 12 September 2026

The original clean worker at `0ed36d145d471887b3462b8c756189d79450b374` remains
intact. Only the five approved Services source paths and their existing QA harness
were ported from common base `552aaaf0` using a three-way patch onto published
`fb26f3dc`. Existing later global styles and other application work are preserved.
The Services CSS delta is confined to its band styles. Source formatting follows
current repository rules.

The earlier independent review accepted the content and anchor coverage but found
blank lower regions in normal-motion full-page screenshots. The existing QA now
scrolls through every revealed section, checks settled heading/content opacity,
reveals nested process steps and footer groups, and captures each region before
the full-page image. Desktop, mobile and reduced motion retain real keyboard focus,
a real Build anchor click, overflow, missing-anchor and console checks.

`--services-only` runs this bounded part of the existing public navigation harness
in the existing CI server. CI retains the section screenshots and summary. The
analytics beacon is explicitly stubbed in this fictional verification environment;
no real analytics success is claimed. The old broader navigation mode remains.

## Evidence boundary

Scoped lint/syntax, house-style and fabricated-claim tests, agent contract and source
statistics are checked locally. Final remote build/CI and opened desktop/mobile
lower-page screenshots remain required before accepting AC1–3.

## Public content review

The page and supporting service/anchor content follow the existing four engagement
modes and custom-work positioning. The public changelog reflects the clearer offer.
Organized platform guides and Command Center/FAQ text retain their existing product
promise because this page adds no platform operation or provider capability. Command
Center remains one optional solution in the broader service offer. No production
application deployment is part of this recovery.
